clusterName: spoke-prod
labels:
  environment: prod
  tenant: default
  client_provisioner: keycloak
# TODO(env-isolation): shared "platform" realm — not isolated from dev/hub yet.
clients:
  - name: agentgateway          # → clientId "agentgateway-prod"
    type: confidential
    flows:
      - client_credentials
